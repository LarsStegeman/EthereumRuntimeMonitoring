monitor myContract{
    DEA testMonitor {
        states {
            State: initial;
            
        }
        transitions {
            State -[number@( number > 0)]-> State;
        }
    }
}